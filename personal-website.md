# 个人作品集 / 品牌官网设计 Brief v3.0

> 使用方式：只填写第 0 节的项目配置；其余内容是执行规则。方括号 `[...]` 为待填项。

## 任务

为 **[品牌 / 个人名称]**（[身份 / 职业]）设计并实现一个 **[项目类型]** 网站。它服务于 **[主要目标用户]**，以 **[设计主题]** 为视觉方向。

目标：让访客在 **[时间，如 30 秒]** 内看懂“[你是谁 / 你提供什么价值]”，并完成 **[浏览作品 / 联系 / 下载简历 / 预约]**。

项目为 **[纯前端 HTML+CSS+JS / Vue / React / Next.js]**；默认只做本地预览，**未经明确授权不得部署、发布或替换真实内容**。

---

## 0. 项目配置（唯一必填区）

```yaml
项目:
  名称: [品牌 / 个人名]
  类型: [个人作品集 / 品牌官网 / 摄影集 / 案例集]
  身份与一句话定位: [职业、年限、核心价值]
  目标动作: [联系 / 浏览案例 / 下载简历 / 预约]

作者画像:
  年龄或年龄段: [如 32 岁 / 30–35 岁；可不填]
  性别或性别认同: [可不填]
  职业与行业: [职业、所在行业]
  职业阶段: [新人 / 成长期 / 资深 / 转型期 / 创业者]
  地区与文化语境: [城市 / 国家 / 面向国内或国际；可不填]
  个性与表达方式: [克制 / 外向 / 理性 / 感性 / 实验等]
  希望建立的公众印象: [专业可信 / 有创造力 / 有亲和力 / 有领导力等]
  不希望呈现的形象: [过度年轻化 / 商务模板感 / 网红感等]

受众:
  主要: [谁；最在乎什么]
  次要: [谁；最在乎什么]
  设计取舍: [深度 / 广度 / 速度 / 情绪，选一个优先]

品牌:
  关键词: [[关键词 1], [关键词 2], [关键词 3]]
  气质参考: [[编辑杂志 / 美术馆 / 沉浸纪录片 / 私人日记等]]
  反例气质: [不要做成什么]
  必须保留: [[姓名 / 品牌名 / 联系方式 / 关键经历 / 其他]]

内容资产:
  图片: [路径、数量、格式、是否均为真实素材]
  视频: [路径或无]
  文档: [简历 / 案例 PDF / 无]
  真实数据: [奖项、业绩、媒体报道；没有则留空]
  字体: [可用字体或系统字体]
  内容规划: [用户提供 / AI 规划演示内容 / 共同规划]

技术与预览:
  技术栈: [HTML+CSS+JS / Vue / React / Next.js]
  字体来源: [自托管 / 已授权在线字体 / 系统字体]
  图片策略: [原图优先；WebP / AVIF 衍生图策略]
  预览入口: [http://localhost:PORT/]
  部署: [暂不部署 / 已明确授权的平台]
```

---

## 1. 角色与原则

你同时承担视觉设计、交互设计、创意前端实现和体验验收四个职责。优先级依次为：

1. 真实内容与品牌准确性；
2. 清晰的叙事和浏览路径；
3. 克制、有辨识度的视觉；
4. 可访问性、性能和跨端稳定性。

不要用炫技代替内容，不要用模板组件代替版式设计，也不要为了“完整”编造作品、数据、客户、奖项、评价或人物故事。

### 内容自主规划门槛（必须执行）

当用户说“你来决定内容”、无法自行设计网站内容，或只给出题材而没有页面方案时，不能只做一个好看的首屏或简单视觉样稿。必须先把题材转译成一份可浏览的内容产品：

1. 明确网站定位、目标用户、核心动作和内容叙事主线；
2. 规划完整信息架构、导航关系、页面清单和每页职责；
3. 为每个核心页面补齐演示级内容，包括标题、导语、正文、案例 / 作品、元数据、按钮文案、空状态和联系方式；
4. 把内容拆成可替换的数据结构，确保用户日后能用真实资料替换演示内容；
5. 让每个导航入口都通向真实内容或明确的空状态，不得出现假按钮、空链接或只放标题的页面；
6. 静态方向只是视觉决策稿。用户选定方向后，必须继续实现完整网站，不能把方向稿当作最终交付。

如果关键真实资料缺失，使用与题材匹配的“演示内容 / 待替换”并标明边界；不得捏造可核验的客户、奖项、数据、联系方式或履历。

---

## 2. 硬约束

### 内容与素材

- 真实姓名、品牌名、联系方式和「必须保留」字段不得遗漏或改写含义。
- 先盘点素材。已有真实图片必须优先使用原图；图片缺失、损坏或路径不通时先报告，不能拿随机图替代。
- 没有真实内容的模块直接隐藏；仅当用户要求展示结构时，才使用清楚标为“临时”的占位内容。
- 占位图只能用于布局验证，必须标注替换位置；不得生成或伪造最终作品。
- 默认中文。英文只用于品牌名、作品原名和用户明确提供的内容；图片说明保持一两句，避免长文案墙。

### 视觉红线

- 不要蓝紫渐变、玻璃拟态、圆角阴影卡片墙、默认 Bootstrap/Tailwind UI 气质。
- 不要粒子背景、无功能抽象 3D、满屏 WebGL、无意义噪点或 emoji 图标。
- 不要“所有元素一起放大缩小”的滚动动画；不用装饰填满留白。
- 参考案例只能借鉴信息架构、节奏或交互逻辑；不得复制其品牌、图片、文案、Logo、代码或完整布局。

### 设计系统

- 基础主色不超过 3 个，另加不超过 2 个中性色；系列强调色仅少量使用。
- 字体不超过 3 种（含中英文），确定后全站一致。
- 页面默认只允许纵向滚动，桌面端与移动端均不得出现横向溢出。

---

## 3. 执行门禁（必须按阶段停下）

### 阶段 A：先理解需求，暂不找案例、不做页面

输出一份简洁的设计判断，包含：

- 内容结构、页面信息架构和核心叙事；
- 素材清单：文件名、类型、尺寸、所属系列、真实 / 临时、计划位置；
- 作者画像：年龄、性别、职业、职业阶段、个性和希望建立的公众印象；
- 目标受众与设计取舍；
- 3 个视觉关键词与一句成功标准；
- 必须保留的信息、明确红线，以及发现的缺失素材、冲突或待确认项。
- 如果用户没有内容方案：补充一份“内容规划草案”，列出网站定位、导航、页面清单、每页内容模块和演示内容边界。

成功标准写成一句可验证的话：
> [目标用户] 在 [时间] 内完成 [动作]，并能感知到 [预期认知 / 情绪]。

输出一张“画像 → 设计影响”表，至少覆盖内容重点、视觉气质、信息密度、信任建立和互动方式：

| 画像信息 | 对网站规划的影响 | 具体设计动作 | 需要避免 |
|---|---|---|---|
| [如资深建筑师、职业转型期] | [突出方法论与可信度] | [案例先于自我介绍，增加过程证据] | [泛化商务模板] |

年龄和性别只能作为作者主动提供的背景信息，不得根据姓名、照片或职业推测；不得使用“男性就硬朗、女性就柔美、年轻就潮、年长就保守”等刻板映射。若画像信息与职业目标冲突，优先服从作者明确表达的定位和目标。

此阶段的目的只是准确复述和确认项目需求；**不凭通用审美预设方向，不开始写页面，也不先从参考站挑案例。**

### 阶段 B：根据已理解的需求，定向寻找参考案例

基于阶段 A 的作者画像、受众、内容类型、品牌气质、技术边界和红线，在参考网站或公开网页中寻找真正适合本项目的案例。优先寻找职业、职业阶段、内容形态和目标接近的作品；年龄与性别只作为辅助背景，不能成为筛选案例的唯一依据，也不按网站清单机械浏览。

输出一张参考借鉴表，每个案例说明：

| 案例 | 为什么适合本项目 | 可借鉴的具体点 | 明确不照搬的部分 | 准备落地到哪里 |
|---|---|---|---|---|
| [名称 / 链接] | [与受众、内容或气质的关联] | [如目录、图片叙事、转场节奏] | [品牌、文案、完整布局等] | [首页 / 列表 / 详情] |

- 只保留能支撑设计决策的 3–6 个案例；找不到合适案例时如实说明，不用不匹配的案例凑数。
- 案例只能作为局部方法证据，最终视觉必须围绕本项目的真实内容重建。
- 完成后给出“参考研究结论”：哪些设计选择被支持、哪些常见做法需要避开。

### 阶段 C：基于需求与参考研究，只做三个静态方向

制作三个差异显著的 1440×900 首页静态方向（可为 HTML 可预览稿或高保真静态图），每个方向说明：名称、叙事逻辑、色彩 / 字体、图片呈现、交互基调，以及它如何体现作者的职业、职业阶段、个性与目标形象。

三个方向必须至少在以下 4 项上有实质区别：构图、排版密度、色彩关系、图片叙事、目录结构、阅读节奏、动效语言。**不能只换颜色或字体。**

每个方向都要标出它响应了哪些项目需求、参考了哪一种“方法”而非哪一个页面。完成后停止，等待用户选定方向。此阶段不得直接扩展为完整网站。

### 阶段 D：基于选定方向实现

默认页面集合按内容裁剪：

- 首页：定位、精选内容、清晰的下一步；
- 作品 / 分类页：筛选或目录；
- 系列列表与详情：背景、过程、结果、反思（有内容才展示）；
- 关于与联系页；
- 图片查看层。

每个系列依据内容选择一种最合适的叙事结构，例如固定主画面随滚动换内容、纵向胶片、非机械式分镜、分层图文或主图逐步展开。**同一站点最多使用 2 种核心浏览模式**；有多个系列时再做有理由的差异化，不能为“不同”而不同。

#### 完整网站的最低内容门槛

- 首页必须交代“是谁 / 做什么 / 为什么值得继续看 / 下一步做什么”；
- 目录中的每个可见入口必须有对应内容，不能用“后续预留”凑数量；
- 每个核心作品 / 案例至少包含背景、过程或方法、结果 / 交付和一句简短说明；
- 个人类网站至少包含简介、经历或能力、服务 / 合作方式和联系方式；
- 作品图片不足时减少模块数量，不用重复图片或装饰性空白伪装成完整；
- 演示内容要集中、具体、可替换，并在交付说明中列出需要用户补充的真实资料。

### 阶段 E：验收与交付

在真实浏览器中完成桌面与移动端检查，修复发现的问题后再交付。不要把构建成功或 HTTP 200 当作完整验收。

---

## 4. 交互、可访问性与性能

### 必需体验

- 图片悬停反馈（仅支持指针设备时启用）；
- 点击主图放大查看，支持 ESC 关闭、关闭按钮、焦点管理与键盘操作；
- 分类 / 系列切换有短促且可理解的过渡；
- 滚动进度或当前位置反馈；
- 图片懒加载；
- 移动端触控可用，不依赖 hover 才能完成关键操作；
- 支持 `prefers-reduced-motion`，降动效后内容和操作仍完整可用。

动效必须说明内容，单页只保留少量主交互。材质适合细节放大，人物 / 时装适合镜头推进和裁切变化，建筑适合分层与线条展开；不匹配时宁可不用。

### 实现边界

- 优先 `transform`、`opacity`、CSS variables、`requestAnimationFrame` 与 `IntersectionObserver`。
- `clip-path`、`mask`、`filter` 等可用，但只在必要时使用，并在目标设备上验证滚动表现。
- 避免高频状态更新、反复读写布局、大面积模糊、未压缩大图、动画闪烁和滚动卡顿。
- 一次只完成并验证一个交互模块，避免把未验证的特效叠加到页面中。

---

## 5. 决策与授权边界

| 场景 | 行为 |
|---|---|
| 视觉方向、品牌气质、内容取舍 | 先给选项，等待确认 |
| 颜色、字号、栅格、微交互细节 | 自行判断，交付时说明 |
| 缺失素材、替换真实内容、关键文案含义 | 先询问 |
| 明显的视觉缺陷、性能与无障碍修复 | 可直接修复并报告 |
| 部署、上线、发布、外部服务配置 | 仅在用户明确授权后执行 |

---

## 6. 验收标准

| 项目 | 标准 |
|---|---|
| 响应式 | 360 / 768 / 1024 / 1440 宽度均可用 |
| 内容 | 真实素材正常加载；无虚构内容；图片比例正确 |
| 内容完整度 | 首页、导航、核心内容、关于 / 能力、联系和空状态均有明确职责；不可见模块不占位 |
| 导航与交互 | 可浏览、分类切换正常；图片可放大；ESC 可关闭；键盘可操作 |
| 视觉 | 不出现禁用的模板化特征；留白与层级服务内容 |
| 布局 | 无横向滚动，无明显遮挡、跳动或闪烁 |
| 性能 | 首屏和连续滚动流畅；图片按需加载 |
| 浏览器检查 | Chrome、Safari、Edge 最近两版；控制台错误 0、关键请求失败 0 |
| 降低动态效果 | 启用后内容、导航和图片查看仍可用 |
| 工程 | 构建成功；入口 HTML、脚本、字体和图片按合理方式管理；依赖仅保留必要项 |

---

## 7. 交付物

1. 需求理解与素材盘点（阶段 A）；
2. 定向案例研究及借鉴表（阶段 B）；
3. 三个静态方向及对比说明（阶段 C，供选择）；
4. 选定方向后的完整源码与目录说明；
5. 本地预览入口；
6. 素材清单：真实 / 临时 / 替换方法；
7. 交互说明：触发条件、效果、降动效行为；
8. 浏览器验收记录：首页、详情、图片放大、移动端截图，以及控制台 / 网络检查结论；
9. 验收清单：通过 / 未通过 / 不适用，并说明遗留项。

---

## 8. 参考素材（按需查阅）

### 整体气质

- Studio Krave：https://mehditerzi.com/projects/studio-krave
- ITom：https://itomdev.com/
- Trevor Noah：https://www.trevornoah.com/
- LAX Space：https://www.laxspace.co/

### 局部结构与交互

- Framer Gallery：https://www.framer.com/gallery/
- One Page Love：https://onepagelove.com/
- Lapa Ninja：https://lapa.ninja/
- Landing Love：https://www.landing.love/
- Mobbin：https://mobbin.com/
- Land-book：https://land-book.com/

### 只作组件与趋势备查

- Awwwards：https://www.awwwards.com/
- SiteInspire：https://www.siteinspire.com/
- Open Design：https://github.com/nexu-io/open-design

### 抖音视频中的灵感来源

以下链接只用于理解视频中提到的设计资源和动效方法，不复制视频作者的页面、文案或代码：

- 海明Dev《10 个设计网站拯救你的 AI 审美》：https://v.douyin.com/f4EwgRBHfBQ/
- AI趋势报《GitHub 神级项目一键复刻》：https://v.douyin.com/Pfbmr9gHVoM/
- AI教练振轩《拯救你 AI 审美的 5 个宝藏网站》：https://v.douyin.com/OV9wtx33_Rc/

先根据项目内容决定要学什么，再看参考；禁止从灵感站拼贴出一个没有项目气质的网站。

---

## 完成前自检

- [ ] 已盘点素材，所有真实内容已优先使用
- [ ] 已将作者画像转化为具体设计判断，且没有使用年龄或性别刻板印象
- [ ] 用户没有内容方案时，已先完成内容规划草案，而不是直接做视觉样稿
- [ ] 首页、导航、核心内容、关于 / 能力、联系和空状态均有明确内容职责
- [ ] 每个核心作品 / 案例都有背景、方法或过程、结果 / 交付和简短说明
- [ ] 已先理解并复述项目需求，再按需求完成定向案例研究
- [ ] 已将案例研究结论转化为三个有实质区别的静态方向，并在此处等待用户选择
- [ ] 选定方向后才开始完整实现
- [ ] 没有未经确认地替换内容、编造素材或部署
- [ ] 核心交互在鼠标、键盘与触控设备上都可用
- [ ] 已在真实浏览器完成桌面、移动端、控制台与网络检查
- [ ] 已交付素材、交互和验收记录
